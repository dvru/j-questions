// Write a function that takes 2 numbers and returns the maximum of the two.

let number = max(3, 3)
console.log(number)

function max(num1, num2){
    // if(num1 > num2) return num1;
    // return num2;

    return(num1 > num2) ? num1 : num2;

}