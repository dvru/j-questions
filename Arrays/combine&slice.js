const first = [1, 2, 3];
const second = [4, 5, 6];

const combined = first.concat(second); // combine 2 arrays
console.log(combined);

const slice = combined.slice(2); // slice 2 arrays
console.log(slice);


